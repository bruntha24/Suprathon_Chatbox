{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "uvicorn server:app --reload --host 127.0.0.1 --port 8000",
    "start": "uvicorn server:app --host 127.0.0.1 --port 8000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "nodemon": "^3.1.14"
  }
}
